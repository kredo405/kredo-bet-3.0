import axios from 'axios';

function calcPrediction(outcomes, match, odds, dataForPrediction, prediction) {
  console.log(outcomes, odds, dataForPrediction);
  console.log(prediction);
}

export default calcPrediction;
